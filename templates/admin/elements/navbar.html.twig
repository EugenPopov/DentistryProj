<header class="header">
    <nav class="navbar">
        <div class="search-box">
            <button class="dismiss"><i class="icon-close"></i></button>
            <form id="searchForm" action="#" role="search">
                <input type="search" placeholder="What are you looking for..." class="form-control">
            </form>
        </div>
        <div class="container-fluid">
            <div class="navbar-holder d-flex align-items-center justify-content-between">
                <div class="navbar-header">
                    <a href="{{ path('admin_index') }}" class="navbar-brand d-none d-sm-inline-block">
                        <div class="brand-text d-none d-lg-inline-block"><span>Стомасвіт </span><strong> Admin</strong></div>
                        <div class="brand-text d-none d-sm-inline-block d-lg-none"><strong>BD</strong></div></a>
                    <a id="toggle-btn" href="#" class="menu-btn active"><span></span><span></span><span></span></a>
                </div>
                <ul class="nav-menu list-unstyled d-flex flex-md-row align-items-md-center">
                    <li class="nav-item dropdown"> <a id="notifications" rel="nofollow" data-target="#" href="#" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" class="nav-link"><i class="fa fa-bell-o"></i><span class="badge bg-red badge-corner application-amount"></span></a>
                        <ul aria-labelledby="notifications" class="dropdown-menu">
                            <li><a rel="nofollow" href="{{ path('application_index') }}" class="dropdown-item">
                                    <div class="notification">
                                        <div class="notification-content">
                                            <i class="fa fa-envelope bg-green"></i>
                                            У вас <span class="application-amount-li" style="font-size: unset; color: unset;"></span> новых заявок
                                        </div>
                                    </div>
                                </a>
                            </li>
                            <li><a rel="nofollow" href="{{ path('application_index') }}" class="dropdown-item all-notifications text-center"> <strong>Смотреть все заявки                                           </strong></a></li>
                        </ul>
                    </li>
                    <li class="nav-item"><a href="{{ path('app_logout') }}" class="nav-link logout"> <span class="d-none d-sm-inline">Выйти</span><i class="fa fa-sign-out"></i></a></li>
                </ul>
            </div>
        </div>
    </nav>
</header>